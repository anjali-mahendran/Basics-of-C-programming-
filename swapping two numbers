#include<stdio.h>
int main()
{
    int n,m,temp;
    scanf("%d%d",&n,&m);
    printf("before swapping the number is %d and %d\n",n,m);
    temp=n;
    n=m,
    m=temp;
    printf("after swapping the number is %d and %d",n,m);
}
