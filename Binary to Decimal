#include<stdio.h>
int main()
{
    int n,base=1,rem,dec=0;
    scanf("%d",&n);
    while(n)
    {
        rem=n%10;
        dec=dec+rem*base;
        n=n/10;
        base=base*2;
    }
    printf("the decimal value is %d",dec);
}
